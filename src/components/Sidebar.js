
import React from 'react';

const Sidebar = () => {
    return (
        <div className="sidebar">
            <h2>Realtime</h2>
            <button>Logs</button>
            <button>Settings</button>
        </div>
    );
};

export default Sidebar;
